package com.test.begin.di1;

public class Main {
	
	public static void main(String[] args) {
		
		// 목적] Hong에게 일을 시키자!!
		// Main > Hong(X)
		// Main > Service > Hong(O)
		// Service > 중간관리자 역할
		
		
		//Hong hong = new Hong(); > 일케 안함
		
		//Main >(위임) > Service > (위임) > Hong
		//Main <->(의존관계) > Service <-> (의존관계) > Hong
		
		
		
		
		Service service = new Service();
		
		service.work();
		
		
	}

}
